// "q7X-2049 / 🪐 / 바람... 314159" -> Ambient
// identity: C minor pentatonic · distant weather · 65 BPM
// mood: dark, steady, intricate
// form: fade · bloom · drift · dissolve (16 cycles)
// variation: H0 M0 G0 A0

setcpm(65/4)

const harmony = chord("<Cm7 G#^7 Fsus A#>").dict("ireal")

// bass · roots that follow harmony
$: n("<[0 0 2 1] [0 0 1 ~] [0 ~ 1 ~] [0 1 0@2]>")
.set(harmony).mode("root:g2").voicing()
.s("triangle")
.lpf(298).attack(.2).release(.8).gain(.3).room(0.33)
.orbit(2)
.mask("<1@6 0@2 1@6 0@2>")

// harmony · voice-led progression
$: harmony.anchor("c3").voicing()
.struct("x")
.s("triangle")
.attack(.5).release(1.5)
.lpf(sine.range(523,1590).slow(16))
.room(0.65).gain(.15)
.orbit(3)
.mask("<1@7 0@1 1@6 0@2>")

// lead · motif, answer, resolution
$: n("<[1@4 ~ ~ 2@2] [~ ~ 0@4 ~ ~] [1@4 ~ ~ 2@2] [2@2 1@2 1@2 ~ ~] [1@4 ~ ~ 2@2] [~ 1 1@4 ~ ~] [1@4 ~ ~ 2@2] [2@2 1@2 0@2 ~ ~]>").set(harmony)
.anchor("c4").voicing()
.s("sine")
.attack(.2).release(1)
.delay(.5).delayfeedback(.5)
.room(0.65).gain(.3)
.lpf(3370)
.lpq(2)
.hpf(20)
.shape(0)
.crush(16)
.velocity("<1 .88 .94 .82>")
.orbit(4)
.mask("<0@2 1@6 0@2 1@4 0@2>")

// accent · harmony-derived arpeggio
$: n("[0 1 2 1]*2").set(harmony)
.anchor("c5").voicing()
.s("sine")
.decay(.08).sustain(0)
.delay(.18).delayfeedback(.28)
.room(0.35).gain(.14)
.orbit(5)
.mask("<0@5 1@3 0@4 1@2 0@2>")

// texture · air around the arrangement
$: s("brown")
.lpf(554)
.gain(sine.range(.02,0.06).slow(14))
.room(0.55)
.orbit(6)
.mask("<1@4 0@2 1@8 0@2>")

// --- generated arrangement end ---
